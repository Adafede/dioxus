[package]
name = "json-count-rs"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"
description = "Web app for counting non-null JSON fields from uploaded files"
license = "AGPL-3.0-only"
keywords = ["dioxus", "json", "wasm", "web"]
categories = ["wasm", "web-programming"]
publish = false

[[bin]]
name = "json-count-rs"
path = "src/main.rs"

[dependencies]
dioxus = { workspace = true }
file-upload = { path = "../../crates/file-upload", version = "0.1" }
gloo-timers = { version = "0.3", features = ["futures"] }
shared = { path = "../../crates/shared", version = "0.1" }
ui = { path = "../../crates/ui", version = "0.1" }

[target.'cfg(target_arch = "wasm32")'.dependencies]
js-sys = { workspace = true }
wasm-bindgen = { workspace = true }
wasm-bindgen-futures = "0.4"
web-sys = { workspace = true, features = ["Blob", "File", "FileList", "FileReader", "HtmlInputElement"] }

[lints]
workspace = true
