[package]
name = "ui"
version = "0.1.0"
edition = "2021"
authors = ["Dioxus Contributors"]
license = "AGPL-3.0-only"
description = "Unified UI design system for Dioxus applications using pure Rust styling"
repository = "https://github.com/adrutz/dioxus"
publish = false

[dependencies]
dioxus = { workspace = true }
serde = { workspace = true }

[dev-dependencies]
dioxus-web = { version = "0.7", features = ["hydrate"] }
